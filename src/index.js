import './style.css';
import { initializeEvents } from './initEvents.js';

const data = []; // holds object to call on later // possible create a render existing cards for sticky wall tab

function component() {
    initializeEvents();
}
component();

